# Teamarr v2 Configuration
# Copy this file to .env and customize

# User Timezone - THE single source of truth for all time display
# All dates, times, and XMLTV output will use this timezone
# Examples: America/New_York, America/Los_Angeles, Europe/London, Australia/Sydney
USER_TIMEZONE=America/New_York

# Database path (default: ./data/teamarr.db)
# DATABASE_PATH=/path/to/teamarr.db

# API server settings
# API_HOST=0.0.0.0
# API_PORT=8000

# ESPN API base URL (usually no need to change)
# ESPN_API_BASE=https://site.api.espn.com/apis/site/v2/sports

# TheSportsDB API key (default: free tier key "123")
# Get a premium key from https://www.thesportsdb.com/members
# TSDB_API_KEY=123

# Logging Configuration
# LOG_LEVEL: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
LOG_LEVEL=INFO

# LOG_DIR: Directory for log files (default: ./logs or /app/data/logs in Docker)
# LOG_DIR=/path/to/logs

# LOG_FORMAT: "text" or "json" (default: text)
# Use "json" for log aggregation systems (ELK, Loki, etc.)
# LOG_FORMAT=text
