services:
  teamarr:
    image: ghcr.io/egyptiangio/teamarr:v2-alpha
    container_name: teamarr
    restart: unless-stopped
    ports:
      - 9195:9195
    volumes:
      - ./data:/app/data
    environment:
      - TZ=America/Detroit
